# Neural Nexus UI Configuration
# Copy this file to .env and modify as needed

# Ollama API endpoint (default: uses Vite proxy to localhost:11434)
# Leave empty to use the default proxy, or set full URL for remote Ollama
VITE_OLLAMA_ENDPOINT=

# Default port for development server (default: 3000)
VITE_PORT=3000
